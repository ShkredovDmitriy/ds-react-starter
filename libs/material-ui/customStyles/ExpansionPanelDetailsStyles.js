import theme from "../customTheme/themes";

const ExpansionPanelDetailsStyles = ((theme) => {
  return {
    /* Styles applied to the root element. */
    root: {
        display: 'flex',
        padding: '8px 24px 24px'
    }
  };
})(theme);

export default ExpansionPanelDetailsStyles;
