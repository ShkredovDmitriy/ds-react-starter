import theme from "../customTheme/themes";

const DialogTitleStyles = ((theme) => {
  return {
    /* Styles applied to the root element. */
    root: {
        margin: 0,
        padding: '16px 24px',
        flex: '0 0 auto'
    }
  };
})(theme);

export default DialogTitleStyles;
