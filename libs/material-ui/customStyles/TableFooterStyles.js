import theme from "../customTheme/themes";

const TableFooterStyles = ((theme) => {
  return {
    /* Styles applied to the root element. */
    root: {
        display: 'table-footer-group'
    } 
  };
})(theme);

export default TableFooterStyles;
